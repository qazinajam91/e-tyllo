name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Setup Debug Keystore Location
      run: |
        mkdir -p ~/.android
        keytool -genkey -v -keystore ~/.android/debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"

    - name: Unzip Source Code & Fix Permissions
      run: |
        unzip -o "e-tyllo (4).zip" -d extracted_app
        # If the zip extracted inside another subfolder, locate gradlew and make executable
        find extracted_app -name "gradlew" -exec chmod +x {} +

    - name: Build APK with Gradle Wrapper
      run: |
        # Find the directory containing gradlew dynamically
        GRADLE_DIR=$(find extracted_app -name "gradlew" -exec dirname {} \;)
        cd "$GRADLE_DIR"
        ./gradlew assembleDebug --no-daemon

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Etyllo-Expense-APK
        path: extracted_app/**/build/outputs/apk/debug/*.apk
